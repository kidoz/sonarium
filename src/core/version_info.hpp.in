#pragma once

// Generated by Meson's vcs_tag() at build time — do not edit or include
// outside version.cpp. @VCS_TAG@ is `git rev-parse --short HEAD`, or the
// fallback when building outside a git checkout.
#define SONARIUM_GIT_REVISION "@VCS_TAG@"
